package main.scorer

import main.framework.Score
import main.models.Cache

object Scorer {
  def compute(caches: List[Cache]): Score = {
    val result = 0
    val max = 0

    Score(result, max)
  }
}
