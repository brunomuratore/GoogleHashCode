package main.scorer

import main.framework.Score
import main.models.Slice

import scala.collection.mutable.ListBuffer

object Scorer {
  def compute(slices: Int, array: Array[Array[Int]]): Score = {
    val result = 0
    val max = 0

    Score(result, max)
  }
}
